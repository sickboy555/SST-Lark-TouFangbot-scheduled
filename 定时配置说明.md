# 定时发送配置（外部定时器方案）

每天 17:07（北京时间）准时发送预算日报。

## 原理

GitHub 自带的 `schedule` 定时器不准，高负载时段会延迟 1~4 小时（之前就是因为这个，日报拖到晚上 8 点多才发）。
所以改用 **cron-job.org** 这个准时的外部定时器，每天准点去调用 GitHub 的 `workflow_dispatch` 接口，
直接启动 `daily_report.yml` 发报。发报本身仍在 GitHub 云端跑，**电脑关机也照发**。

```
cron-job.org（每天 17:07 北京时间，准到秒）
        │  POST workflow_dispatch
        ▼
daily_report.yml  ──►  发飞书日报
```

当前 workflow 已改为通过外部群“自定义机器人 Webhook”发送日报，不再依赖自建应用机器人加入目标群。
GitHub 仓库的 **Settings → Secrets and variables → Actions** 里需要有这些 Secrets：

| Secret | 用途 |
|--------|------|
| `LARK_APP_ID` | 读取表格、上传图片用的 Lark 自建应用 App ID |
| `LARK_APP_SECRET` | 读取表格、上传图片用的 Lark 自建应用 App Secret |
| `LARK_SHEET_URL` | 预算表格 URL |
| `LARK_WEBHOOK_URL` | 外部群自定义机器人的 Webhook 地址 |
| `LARK_WEBHOOK_SECRET` | 外部群自定义机器人签名密钥；如果未启用签名校验可留空或不配 |

---

## 一、创建 GitHub 细粒度 PAT（一次性）

1. 打开 https://github.com/settings/personal-access-tokens/new
2. **Token name**：随便填，例如 `lark-budget-cron`
3. **Expiration**：建议 1 year（到期前 GitHub 会邮件提醒，到期后来这里重新生成即可）
4. **Resource owner**：选你自己（sickboy555）
5. **Repository access** → 选 **Only select repositories** → 勾选 `SST-Lark-TouFangbot-scheduled`
6. **Permissions** → 展开 **Repository permissions** → 找到 **Actions** → 设为 **Read and write**
   （其它权限一律不用给，最小化）
7. 拉到底点 **Generate token**，复制那串 `github_pat_xxxx`
   - ⚠️ 这串只显示一次，复制好。**不要发给任何人、不要贴进聊天**。
   - 万一泄露：回 https://github.com/settings/tokens 把它 Revoke，重新生成一个。

---

## 二、配置 cron-job.org（一次性）

1. 注册并登录 https://cron-job.org （免费）
2. 右上角头像 → **Settings**，把 **Timezone** 设为 `Asia/Shanghai`，保存
   （这样下面填 17:07 就是北京时间，不用自己换算 UTC）
3. 回主页点 **Create cronjob**，按下面填：

   **Common 区：**
   - **Title**：`飞书预算日报`
   - **URL**：
     ```
     https://api.github.com/repos/sickboy555/SST-Lark-TouFangbot-scheduled/actions/workflows/daily_report.yml/dispatches
     ```

   **Schedule 区：**
   - 选 **Every day**，时间设为 `17:07`
     （或用自定义：分钟 `7`、小时 `17`、其余每天）

   **Advanced 区（点开 Advanced settings）：**
   - **Request method**：`POST`
   - **Request body** / **Custom body**：
     ```json
     {"ref":"main"}
     ```
   - **Headers**（逐条添加，Key / Value）：
     | Key | Value |
     |-----|-------|
     | `Authorization` | `Bearer github_pat_你刚才复制的那串` |
     | `Accept` | `application/vnd.github+json` |
     | `Content-Type` | `application/json` |
     | `X-GitHub-Api-Version` | `2022-11-28` |

4. 保存（**Create**）。

---

## 三、验证（重要，别等到明天）

1. 在 cron-job.org 该任务页面点 **Run now**（立即执行一次）
2. 看 **History / 执行历史**：HTTP 状态码应是 **204 No Content** = 成功
   - 如果是 **401**：PAT 错了或没复制全 → 重新检查 Authorization 头
   - 如果是 **403**：PAT 权限不对 → 确认给了该仓库的 Actions: Read and write
   - 如果是 **404**：URL 里的仓库名/文件名拼错，或 PAT 没勾这个仓库
3. 去 GitHub → 仓库 → **Actions** 标签页，应看到「每日预算日报」被触发并开始跑
4. 看飞书群有没有收到日报

成功后就不用管了，往后每天 17:07 自动发。

---

## 备注

- GitHub PAT 到期后任务会开始报 401，记得续期（重新生成并更新 cron-job.org 里的 Authorization 头）。
- 想临时手动发一次：直接在 cron-job.org 点 **Run now**，或在 GitHub Actions 页面手动 **Run workflow**。
- 发送时间 17:07 写在 cron-job.org 里；GitHub 里的 `daily_report.yml` 已不含任何定时，只认外部触发。
